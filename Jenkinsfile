pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                script {
                    // safe groovy code
                }
            }
        }
    }
}
